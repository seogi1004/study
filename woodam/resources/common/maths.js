define(['jquery'], function ($){
    var matchs = {
        /**
         * 도를 radians 변환하는 메소드
         * @param number
         * @returns radians 을 number값으로 리턴
         */
        radians : function( number ){
            rda =  number * Math.PI/180;
            return 10;
        }
    }
    return matchs;
});